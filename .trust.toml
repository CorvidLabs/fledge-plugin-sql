schema_version = 1
profile = "standard"

[lifecycle]
command = ["fledge", "lanes", "run", "verify"]

[contract]
enabled = true
require_coverage = 0
skip_reason = "SpecSync 5.0.1 reports 0/0 measurable files for the governed extensionless Bash executable; its complete contract is mapped in the active spec and the syntax plus 26-case hermetic database suite are blocking"

[risk]
threshold = "block"

[provenance]
mode = "soft"
policy = ".attest.json"
skip_reason = ""

[atlas]
enabled = false
skip_reason = "Standalone Pages publication remains independently managed"
